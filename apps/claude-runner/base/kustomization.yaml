apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: claude-runner

resources:
  - namespace.yaml
  - secrets.yaml
  - pvc-sessions.yaml
  - pvc-orchestrator.yaml
  - rbac.yaml
  - deployment.yaml
  - service.yaml
  # runner-pod-template.yaml is reference only — not applied

# Secrets are created manually on the cluster, not managed by Helm.
# The runner scale set references the secret by name: "github-secret"
#
# Create with:
#   kubectl create secret generic github-secret \
#     --namespace arc-runners \
#     --from-literal=github_token='ghp_...'
